{% extends "baseadmin.html.twig" %}

{% block body %}
    <div class="container mt-4">
        <h2>{{ playlist.id ? "Modifier la playlist" : "Ajouter une playlist" }}</h2>
        {{ form_start(form) }}
            
            {{ form_widget(form) }}

            <div class="mt-3">
                <button type="submit" class="btn btn-primary">Enregistrer</button>
                <a href="{{ path('admin.playlist') }}" class="btn btn-secondary">Annuler</a>
            </div>

        {{ form_end(form) }}
    </div>
{% endblock %}
{# empty Twig template #}
